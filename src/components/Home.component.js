/*import React from 'react';
import { Typography } from '@mui/material';
//import { useNavigate } from 'react-router-dom';
import Redirect from '../controller/Redirect.functions';
import HandleLogout from '../controller/Logout.controller';
import {Button} from '@mui/material';

const Home = () => {*/

/*    static reloaded = false;

    constructor(props){
        super(props);
        this.state = {loggedin:'a'};
    }

    render(){*/

        /*return(
            if(localStorage.getItem("usuario") == undefined){
                //if(this.state.loggedin == true) this.setState({loggedin:false});
                
                    
                Redirect('../login')
                    
                
            }else{
                
                //if(this.state.loggedin == false) this.setState({loggedin:true});
                
                return(
                    <div>
                        <Typography>Home page</Typography>
                        <Button onClick={HandleLogout}>Logout</Button>
                    </div>
                    
                );
            }
        );*/
    
//}

/*const Navegar = () => {
    let navigate = useNavigate();
    
    return (<div>{navigate("login")}</div>);
}*/

//export default Home;